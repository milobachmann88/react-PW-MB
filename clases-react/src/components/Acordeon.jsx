export default function Acordeon(props){
    const {titulo} = props;
    const {contenido} = props;
    const [abierto, setAbierto]= useState(false)
    function acordeon(){
        if 
    }
    return(
        <>
        <div>
            <button onClick={contenido}>{titulo}</button>
        </div>
        </>
    )
}